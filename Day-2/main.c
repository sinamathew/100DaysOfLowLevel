#include <stdio.h>
#include "header.h"

/**
 * main - Entry point
 * Description: print out string with _printchar
 * Return: Always 0 (Success)
 */

int main(void)
{
	char *str = "Hello, Alkebulan!\n";

	_printchar(str);
	return (0);
}
