#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 * Description: Print 'H' to the standard output with putchar
 */

int main(void)
{
	putchar('H');
	putchar('\n');
	return (0);
}
